let names = ["Mark", "Lisa", "Melody"];
//             0        1        2

// adding elements into the front of the array is always worse than the end of the array

// built-in array methods
/**
 * push -O(1)
 * pop -O(1)
 * shift -O(N)
 * unshift -O(N)
 * concat -O(N)
 * slice -O(N)
 * splice -O(N)
 * sort -O(N*log(N))
 * forEach/map/filter/reduce/etc -O(N)
 */
